node_modules/
.env
__pycache__/
# secrets
.env
.env.*
!.env.example
*.key
*.pem
*.crt
secrets/

# deps & build
node_modules/
dist/
build/
.vite/

# runtime state. The api writes the live roadmap and its backups here when it runs
# outside docker; the tracked copy is server/seed/roadmap.json, and on a server this
# is the /data volume. Committing it would freeze one machine's edits into the repo.
server/data/

# logs & os
*.log
npm-debug.log*
.DS_Store
