# اجرای پروژه روی سرور خودت

سایت دو حالت دارد:

| حالت | کجا | نقشه از کجا می‌آید | کنسول ادمین |
|---|---|---|---|
| **استاتیک** | GitHub Pages | از `src/content/bootcamp.js` داخل باندل | ندارد |
| **زنده** | داکر روی سرور خودت | از API | دارد |

حالت استاتیک همان چیزی است که الان روی Pages بالاست و دست‌نخورده کار می‌کند. چیزی که پایین می‌آید، حالت زنده است.

## بالا آوردن

```bash
cp .env.example .env
# ADMIN_PASSWORD و SESSION_SECRET و رمزهای کادر را عوض کن:
#   openssl rand -hex 32
docker compose up -d --build
```

بعد از چند دقیقه:

- سایت: `http://<server>:8088/` — یک آدرس، سه تب: **فاز صفر**، **نقشه‌ی بوت‌کمپ** و **برنامه‌ی هفتگی**
- کنسول ادمین: `http://<server>:8088/#/admin`
- پنل کادر (منتورها و مسئول برنامه): `http://<server>:8088/#/panel`

روز اول، نقشه فقط صورت پروژه و سه فاز را نشان می‌دهد: فاز یک باز، فاز دو و سه قفل، و **هر نُه هفته قفل**. باز کردنشان کار کنسول است.

پورت را با `WEB_PORT` در `.env` عوض کن.

## روی اینترنت: HTTPS

تا وقتی سایت روی `http://<ip>:8088` است، رمزها و توکن نشست **متن ساده** روی سیم می‌روند
و هر کسی وسط راه می‌خواندشان. برای شبکه‌ی داخلی اشکالی ندارد؛ برای اینترنت نه.

Caddy این را حل می‌کند: خودش از Let's Encrypt گواهی می‌گیرد و تمدید می‌کند و هیچ کانفیگ
TLS دستی لازم ندارد. فقط یک نام دامنه می‌خواهد که به IP همین سرور اشاره کند.

**اگر دامنه نداری**، از [duckdns.org](https://www.duckdns.org) یک زیردامنه‌ی رایگان بگیر
(مثلاً `nimbo-bootcamp.duckdns.org`) و در پنلش IP سرور را بگذار. رایگان است، ثبت‌نامش با
گیت‌هاب/گوگل است، و Let's Encrypt برایش گواهی می‌دهد.

بعد در `.env`:

```
SITE_DOMAIN=nimbo-bootcamp.duckdns.org
SITE_EMAIL=you@example.com
```

و بالا بیاور — دقت کن که فایل کامپوز **عوض** می‌شود، نه اینکه به قبلی اضافه شود:

```bash
docker compose down                                   # نسخه‌ی http را پایین بیاور
docker compose -f docker-compose.https.yml up -d --build
```

از این به بعد `https://nimbo-bootcamp.duckdns.org` — و `http` خودش به `https` می‌رود.

ولوم داده همان قبلی است، پس هفته‌ها و امتیازها و چالش‌ها دست‌نخورده می‌مانند.

**پیش از بالا آوردن، پورت ۸۰ و ۴۴۳ باید از بیرون باز باشند.** ۴۴۳ برای خود سایت و ۸۰ برای
اینکه Let's Encrypt مالکیت دامنه را بررسی کند. اگر ۸۰ بسته باشد گواهی صادر نمی‌شود.

در این حالت ۸۰۸۸ فقط روی `127.0.0.1` باز است — از خود سرور با `curl` در دسترس، از بیرون نه.

اگر گواهی نگرفت، لاگ را ببین:

```bash
docker compose -f docker-compose.https.yml logs caddy | tail -30
```

معمولاً یکی از این دوتاست: دامنه هنوز به IP این سرور اشاره نمی‌کند (`dig +short <دامنه>`)،
یا پورت ۸۰ از بیرون بسته است.

## وقتی کسی رمز را حدس می‌زند

روی `POST /api/auth/login` شمارنده هست، و دو جور می‌شمارد:

- **به ازای هر نام کاربری**، پنج تلاش مهلت دارد؛ بعدش انتظار دوبرابر می‌شود — یک ثانیه،
  دو، چهار... تا سقف نیم ساعت. یعنی حمله‌ی دیکشنری عملاً می‌میرد.
- **به ازای هر آدرس**، سی تلاش. این عدد عمداً بالاست: چهار منتور پشت یک وای‌فای یک آدرس
  دارند و نباید با اشتباه‌کردنِ یکی‌شان، بقیه قفل شوند. این شمارنده وقتی به کار می‌آید که
  کسی دارد کل حساب‌ها را جارو می‌کند.

ورود موفق، سابقه‌ی آن نام کاربری را پاک می‌کند ولی سابقه‌ی آدرس را نه. شمارنده‌ها در حافظه‌اند،
پس ری‌استارت همه را می‌بخشد. با `LOGIN_FREE_TRIES` و `LOGIN_FREE_TRIES_IP` قابل تنظیم است.

پشت پراکسی، `TRUST_PROXY` تعیین می‌کند آدرس واقعی کاربر چند پله عقب‌تر است: با nginx تنها
`1` (پیش‌فرض) و با Caddy جلوی nginx `2` — که فایل HTTPS خودش ست می‌کند. اگر این عدد غلط
باشد، شمارنده به‌جای کاربرها آدرس خودِ پراکسی را می‌بیند و همه با هم قفل می‌شوند.

## چه چیزی کجاست

```
server/                  API (Express) — نقشه، تیم‌ها، استخر چالش‌ها، احراز هویت
  seed/roadmap.json        حالت اولیه: نُه هفته، چهار تیم، سیزده چالش در استخر
  src/throttle.js          شمارنده‌ی تلاش‌های ناموفق ورود
  src/backup.js            کپی روزانه‌ی فایل داده + ساختن CSV
  src/sheets.js            همان داده‌ها به شکل جدول‌های صفحه‌گسترده
src/content/rhythm.js    متن برنامه‌ی هفتگی — پنج روز و کاری که هر روز دارد
docker-compose.yml       حالت http: دو سرویس، api و web
docker-compose.https.yml حالت https: همان دوتا به‌علاوه‌ی Caddy — فایل کاملِ خودش
Caddyfile                گواهی خودکار و ریدایرکت http به https
Dockerfile.web           بیلد Vite + سرو با nginx
nginx.conf               SPA روی /، پراکسی /api به سرویس api
```

nginx هم سایت و هم API را روی یک origin سرو می‌کند، پس نه CORS لازم است و نه آدرس مطلق.

## بدون داکر (وقتی داکر ایمیج نمی‌کشد)

اگر داکرت به رجیستری نمی‌رسد، همین سرور می‌تواند خودش سایت را هم سرو کند — یک پروسه، بدون nginx:

```bash
npm ci
VITE_BASE=/ VITE_API_BASE=/ npm run build

cd server && npm install && cd ..

DATA_FILE=./server/data/roadmap.json \
ADMIN_USER=nimbo ADMIN_PASSWORD=<رمز> SESSION_SECRET=<یک رشته‌ی تصادفی> \
MENTOR_PASSWORD=<رمز منتورها> LEAD_PASSWORD=<رمز مسئول برنامه> \
SERVE_STATIC=./dist PORT=8088 \
node server/src/index.js
```

بعد `http://localhost:8088/` — کنسول ادمین روی `#/admin` و پنل کادر روی `#/panel`.

برای اینکه بعد از ری‌استارت هم بالا بماند، همین را در یک سرویس systemd بگذار یا با `pm2` اجرا کن.

## داده کجا ذخیره می‌شود

کل وضعیت (هفته‌ها، استخر چالش‌ها، اینکه کدام چالش روی کدام هفته و باز شده یا نه) در یک فایل JSON داخل ولوم `roadmap-data` است:

```bash
docker compose exec api cat /data/roadmap.json > backup.json   # بکاپ
docker compose down -v                                          # پاک‌کردن کامل و برگشت به seed
```

دیتابیس عمداً نگذاشتم: حجم داده چند ده کیلوبایت است و هر نوشتن، یک کلیک یک نفر است.

## کنسول ادمین

با `ADMIN_USER` و `ADMIN_PASSWORD` از `.env` وارد می‌شوی. رمز هیچ‌جای کد نیست و توکن نشست ۱۲ ساعت اعتبار دارد.

دو تب دارد:

**فازها و هفته‌ها** — بالا سه فاز با یک دکمه‌ی باز/قفل. تا وقتی فازی قفل است، نه نیازمندی‌هایش دیده می‌شود و نه هیچ‌کدام از هفته‌هایش — حتی اگر آن هفته را باز کرده باشی (کنسول همان‌جا هشدار می‌دهد). پایینش هر نُه هفته، هرکدام با چهار دکمه‌ی وضعیت، چالش‌هایش، مهلت، و دکمه‌ی «همین حالا باز کن».

**استخر چالش‌ها** — سیزده چالش آماده دارد: داده‌های خراب، توابعی که در یک صفحه جا می‌شوند، تست‌های کانتینری، مرگ یک بروکر، میلیون فایل کوچک، تغییر اسکیما، کوئری کند، رمز جامانده در ایمیج، ماشین خالی، دو مسیر و دو عدد، لاگ بی‌فایده، سه‌برابر شدن بار، و کدی که نمی‌توانی توضیحش بدهی. جست‌وجو دارد، هر کدام را باز می‌کنی و ویرایش می‌کنی، و از همان‌جا روی هر هفته‌ای می‌گذاری‌اش.

## پنل کادر: منتورها و مسئول برنامه

آدرسش `#/panel` است و پنج حساب دارد. نقشِ کسی که وارد می‌شود تعیین می‌کند کدام میز باز شود:

| نام کاربری | نقش | تیم |
|---|---|---|
| `saleh` | منتور | سیروس |
| `asgarian` | منتور | استراتوس |
| `tarkashvand` | منتور | کومولوس |
| `amir` | منتور | نیمبوس |
| `lead` | مسئول برنامه | همه |

رمزها فقط در `.env` هستند: `MENTOR_PASSWORD` و `LEAD_PASSWORD` رمز مشترک‌اند و
`STAFF_PASSWORD_SALEH` (و مانندش) رمز شخصی، که بر رمز مشترک می‌چربد. ادمین هم می‌تواند وارد
همین پنل شود و میز مسئول برنامه را می‌بیند.

هر دو میز تب دارند و ترتیبشان از روی اولویت است: چیزی که جاری است و کاری می‌خواهد، اول.
اگر کاری روی زمین مانده باشد، **عددش روی خودِ تب** می‌نشیند — وگرنه تب‌ها فقط اسکرول را
با مشکل بدترِ «کاری که نمی‌بینی چون پشت تبِ بازنشده است» عوض می‌کردند.

**میز منتور** — سه تب: «ارزیابی شنبه» (پیش‌فرض، با تعداد نفراتی که هنوز ارزیابی نشده‌اند)،
«ترکیب تیم»، «مشاهده‌ها». پیام مسئول برنامه بالای تب‌ها می‌ماند و همیشه دیده می‌شود.

در «ترکیب تیم» هر نفر یک کارت با «چارت قدرت» دارد که حالا فقط خواندنی است و آخرین
ارزیابی همان نفر را نشان می‌دهد.

تب اول هسته‌ی کار است: هفته را انتخاب می‌کند، بعد **یک نفر در هر لحظه**.
کنار هر معیار سه چیز هست — سؤالی که باید بپرسد، نمره‌ای که هفته‌ی قبل به همان نفر داده
(روی همان نوار، با کادر خط‌چین)، و تفاوت این دو. قضاوت‌کردنِ «تغییر» کاری است که آدم
صادقانه می‌تواند بکند؛ گفتن اینکه کسی «۷ از ۱۰ می‌فهمد» نه. اگر نمره‌ای پایین آمده باشد،
تا ننویسد چه چیزی جا نیفتاد، دکمه‌ی ثبت باز نمی‌شود.

پایین همان فرم، **دفاع این هفته**: سؤال بنیادینی که پرسیده شد و اینکه جواب داد، نیمه، یا
نتوانست. این همان سیگنالی است که قبلاً فقط از سناریوهای چالشی درمی‌آمد و جایی ثبت نمی‌شد.

**میز مسئول برنامه** — چهار تب:

**۱. این هفته** (پیش‌فرض) — تنها صفحه‌ای که درباره‌ی «حالا» است نه درباره‌ی کل برنامه: هفته‌ی
جاری، اینکه هر منتور چند نفر از تیمش را ارزیابی کرده و **اسم آن‌هایی که مانده‌اند**، شکاف‌های
همین هفته، و کسانی که هنوز تصمیمی ندارند. عددِ روی این تب تنها عددی است که اگر کسی پیگیری
نکند بی‌سروصدا کهنه می‌شود، پس از هر تب دیگری هم دیده می‌شود.

**۲. ارزیابی یادگیری** — سه نما:

- **چه چیزی جا نیفتاد** — شکاف‌هایی که منتورهای مختلف نوشته‌اند، کنار هم. یک شکاف از یک
  منتور یعنی یک نفر کمک می‌خواهد؛ همان شکاف از سه منتور یعنی درسی جا نیفتاده، و آن تصمیمِ
  مسئول برنامه است نه منتورها.
- **پیشرفت افراد** — نقشه‌ی حرارتی نفر × هفته، با ستون روند.
- **مقایسه‌ی تیم‌ها** — هر معیار، تیم به تیم، تا معلوم شود کدام معیار در کل برنامه ضعیف است.

**۳. تیم‌ها** — جدول چهار تیم مرتب بر اساس میانگین، و صفحه‌ی هر تیم: امتیاز معیارها،
یادداشت‌های منتور، مشاهده‌ها، ارسال راهنمایی، و نفرات با تصمیم هرکدام.

**۴. معیارها و پشتیبان** — اضافه/تغییرنام/بازنشستگی معیارها، و گرفتن خروجی.

عدد هیچ تیمی جایی ذخیره نمی‌شود؛ همه‌اش از ارزیابی‌های افراد حساب می‌شود — از جدول لیگ تا
کارت امتیاز هر تیم. پس خلاصه‌ی مدیر هیچ‌وقت نمی‌تواند با چیزی که منتور ثبت کرده اختلاف
پیدا کند.

منتور فقط تیم خودش را می‌بیند: تیم‌های دیگر اصلاً در پاسخ سرور نیستند، نه اینکه پنهان
شده باشند. تصمیمِ ماندن یا نماندن هم فقط دست مسئول برنامه است؛ اگر منتوری همان درخواست را
دستی بفرستد، سرور نادیده‌اش می‌گیرد.

### اسم‌ها، عکس‌ها و معیارها

سیزده نفر با اسم و پرتره‌ی موقت شروع می‌شوند (سه تیم سه‌نفره و یک تیم چهارنفره). پرتره‌ها
فایل تصویری نیستند: از روی شناسه‌ی هر نفر ساخته می‌شوند، پس ثابت‌اند و جایی ذخیره نمی‌شوند.
مسئول برنامه از پنل خودش اسم را عوض می‌کند و آدرس عکس واقعی را می‌گذارد؛ تا وقتی این کار
نشده، کنارِ اسم برچسب «موقت» می‌خورد.

معیارهای چارت قدرت هنوز قطعی نیستند و همین‌طور هم گذاشته شده‌اند: از بخش «معیارها» اسمشان
عوض می‌شود و امتیازهایی که تا آن لحظه داده شده سر جایشان می‌مانند، چون شناسه‌ی محور دست
نمی‌خورد. یعنی می‌شود از همین هفته امتیاز داد و بعداً اسم‌ها را درست کرد.

## پشتیبان‌گیری

داده‌ها دو جا نگهداری می‌شوند و این عمدی است — یکی روی خود سرور و یکی بیرونش.

**خودکار، روی سرور.** سرور هنگام بالا آمدن و بعد روزی یک بار از فایل داده یک کپی با تاریخ
در اسمش می‌گیرد و کنار خودش در `backups/` می‌گذارد. سی نسخه‌ی آخر می‌ماند و قدیمی‌ترها
پاک می‌شوند. چون داخل همان ولوم است، ری‌بیلد کانتینر را رد می‌کند ولی از دست‌رفتن دیسک را نه.
با `BACKUP_KEEP_DAYS` تعداد نسخه‌ها و با `BACKUP_DIR` محلشان عوض می‌شود.

```bash
docker compose exec api ls /data/backups          # چه نسخه‌هایی هست
docker compose exec api cat /data/roadmap.json > backup.json   # کپی دستی
```

**دستی، بیرون از سرور.** در پنل مسئول برنامه بخش «پشتیبان‌گیری» هست با پنج دکمه: چهار فایل
CSV (نفرات و چارت قدرت، امتیازهای هفتگی، مشاهده‌های منتورها، راهنمایی‌ها) و یک فایل JSON از
کل داده. CSVها با دوبار کلیک در اکسل باز می‌شوند و در گوگل‌شیت هم از `File › Import` مستقیم
بالا می‌آیند؛ JSON برای برگرداندن است نه خواندن.

دو ریزه‌کاری که در این فایل‌ها رعایت شده: علامت BOM ابتدای CSV گذاشته می‌شود، وگرنه اکسل
فارسی را به‌هم‌ریخته باز می‌کند؛ و متنی که با `=` یا `+` شروع شود یک آپاستروف جلویش می‌گیرد،
وگرنه صفحه‌گسترده یادداشت منتور را فرمول حساب می‌کند.

اگر خواستی این‌ها خودکار جایی بروند، همان آدرس‌ها با توکن مسئول برنامه از `curl` هم جواب
می‌دهند و می‌شود در یک cron گذاشتشان:

```bash
TOKEN=$(curl -s -X POST https://<دامنه>/api/auth/login \
  -H 'content-type: application/json' \
  -d '{"user":"lead","password":"<رمز>"}' | grep -o '"token":"[^"]*"' | cut -d'"' -f4)

curl -s https://<دامنه>/api/staff/export/people.csv \
  -H "authorization: Bearer $TOKEN" -o people-$(date +%F).csv
```

سینک مستقیم به گوگل‌شیت را عمداً نگذاشتم: به حساب گوگل و OAuth نیاز دارد که از سرور داخل
ایران دردسر است، و یک وابستگی همیشگی به سرویسی بیرونی اضافه می‌کند برای کاری که یک دکمه‌ی
دانلود انجامش می‌دهد.

## چیزی که مرورگر هرگز نمی‌بیند

این تنها دلیل داشتن بک‌اند است:

- هفته‌ی `locked` فقط پنج فیلد می‌فرستد: `id`, `code`, `status`, `phase`, `title`.
- چالشی که باز نشده فقط `id` و `status: draft` می‌فرستد — نه عنوان، نه متن.

یعنی تا لحظه‌ای که دکمه‌ی «همین حالا باز کن» را نزنی، متن چالش در هیچ باندلی و هیچ پاسخی نیست. نویزی که روی گاوصندوق می‌بینند، در خود مرورگر ساخته می‌شود.

## اگر بک‌اند بخوابد

سایت می‌ماند: فرانت‌اند نسخه‌ی باندل‌شده‌ی نقشه را نشان می‌دهد تا API جواب بدهد، و اگر جواب ندهد همان می‌ماند. کنسول ادمین البته کار نمی‌کند.

## به‌روزرسانی متن هفته‌ها

متن‌ها در `src/content/bootcamp.js` هستند و seed از روی همان ساخته می‌شود:

```bash
node server/scripts/build-seed.mjs
docker compose up -d --build
```

ولوم موجود دست‌نخورده می‌ماند؛ seed فقط وقتی خوانده می‌شود که فایل داده وجود نداشته باشد. برای شروع از نو: `docker compose down -v`.

اگر فایل داده‌ات از نسخه‌ی قدیمی‌تر مانده و کلیدی مثل `phases` یا `teams` را ندارد، سرور آن را از seed پر می‌کند و بقیه‌ی کارت (چالش‌ها، وضعیت هفته‌ها، امتیازها) دست‌نخورده می‌ماند. اسم و تیم و نقشِ حساب‌های کادر هم از seed می‌آید، نه از فایل داده — پس اضافه‌کردن یک منتور یعنی یک تغییر در `src/content/people.js` و یک `build-seed`، نه دست‌بردن در دیتا.

## به‌روزرسانی خودکار از گیت‌هاب

هر push روی `main` ورک‌فلوی `.github/workflows/deploy-server.yml` را می‌اندازد: اول همین‌جا
بیلد می‌شود و API بالا می‌آید تا سالم بودنش معلوم شود، بعد `scripts/deploy.sh` از راه ssh
روی سرور اجرا می‌شود.

**داده در این مسیر پاک نمی‌شود.** پیش از هر بیلدی یک کپی از `/data/roadmap.json` بیرون
ولوم گرفته می‌شود (در `~/nimbo-deploy-backups/`، بیست تای آخر می‌ماند)، و اگر آن کپی JSON
سالمی نباشد اسکریپت همان‌جا می‌ایستد و چیزی را بازسازی نمی‌کند. بعد از `up -d --build`
دوباره چک می‌شود که `/api/health` جواب بدهد، ولوم عوض نشده باشد و فایل داده هنوز خوانده
شود؛ اگر هرکدام رد شود، کامیت قبلی برمی‌گردد بالا.

تنها دستوری که ولوم را نابود می‌کند `docker compose down -v` است و در این مسیر وجود ندارد.

پیش‌نیازها در Settings ← Secrets and variables ← Actions:

| نوع | اسم | توضیح |
|---|---|---|
| secret | `SSH_HOST` | آدرس سرور |
| secret | `SSH_USER` | کاربری که لاگین می‌کند |
| secret | `SSH_KEY` | کلید خصوصی همان کاربر، کل فایل با خط‌های BEGIN/END |
| secret | `SSH_KNOWN_HOSTS` | اختیاری ولی توصیه‌شده — خروجی `ssh-keyscan <host>` |
| secret | `SSH_PORT` | اختیاری، پیش‌فرض ۲۲ |
| variable | `DEPLOY_PATH` | اختیاری، مسیر checkout روی سرور، پیش‌فرض `~/nimbo-final` |
| variable | `COMPOSE_FILE` | اختیاری، پیش‌فرض `docker-compose.yml` |

بدون `SSH_KNOWN_HOSTS` اولین اتصال به هر کلیدی که جواب بدهد اعتماد می‌کند؛ ورک‌فلو در آن
حالت هشدار می‌دهد ولی جلو می‌رود.

سرور باید بتواند خودش از گیت‌هاب `git fetch` کند و روی مسیر deploy یک checkout سالم با
فایل `.env` داشته باشد. دستی هم می‌شود همین را اجرا کرد:

```bash
APP_DIR=~/nimbo bash scripts/deploy.sh <sha>
```

## نقل‌قول‌های داخل چالش

هر چالش با یک جمله از کسی شروع می‌شود که حق گفتنش را داشته، و جمله به همان زبانی می‌ماند که
نوشته شده. قالبش داخل متن چالش این است:

```
> The first rule of functions is that they should be small.
— Robert C. Martin, Clean Code

توضیح ما، به فارسی، زیر نقل‌قول.
```

خطی که با `>` شروع شود نقل‌قول است، خط `—` زیرش گوینده، و بقیه توضیح خودمان. سایت خودش
تشخیص می‌دهد جمله لاتین است و آن یک بلوک را چپ‌به‌راست می‌چیند.
